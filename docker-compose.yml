version: '3.8'

services:
  patch-legislativo:
    build: .
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
    volumes:
      - ./data:/app/data
      - ./.env.local:/app/.env.local
    restart: unless-stopped
    container_name: patch-legislativo
