version: '3.8'

services:
  patch-legislativo:
    build: .
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    container_name: patch-legislativo
